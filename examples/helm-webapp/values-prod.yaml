# Adapted from upstream: added `ingress:` block (new, not in upstream) for
# this environment's host/TLS secret.

replicas: 4

configmap:
  data:
    CUSTOM_HEADER: 'This is on the PROD environment!'

ingress:
  host: helm-prod.kubetest.uk
  tlsSecret: helm-prod-tls
