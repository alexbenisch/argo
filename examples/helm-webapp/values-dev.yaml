# Adapted from upstream: added `ingress:` block (new, not in upstream) for
# this environment's host/TLS secret.

replicas: 5

configmap:
  data:
    CUSTOM_HEADER: "This is on the DEV environment!"

ingress:
  host: helm-dev.kubetest.uk
  tlsSecret: helm-dev-tls
